# Copy to .env.prod (gitignored). Names only — fill locally / via secrets manager.
DOMAIN=localhost
CORS_ORIGINS=https://localhost
NEXT_PUBLIC_API_URL=https://localhost/api/v1
NEXT_PUBLIC_WS_URL=https://localhost
POSTGRES_USER=propos
POSTGRES_PASSWORD=replace-me
POSTGRES_DB=propos
JWT_SECRET=replace-with-long-random
JWT_REFRESH_SECRET=replace-with-long-random
STORAGE_URL_SECRET=replace-with-long-random
PII_ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
DATA_RESIDENCY_REGION=ap-south-1
# Optional digest-pinned base:
# NODE_IMAGE=node:20-alpine@sha256:REPLACE
